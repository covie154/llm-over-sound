---
phase: quick
plan: wf6
type: execute
wave: 1
depends_on: []
files_modified:
  - AHK/include/config.ahk
  - AHK/include/chunking.ahk
  - AHK/include/gui.ahk
  - AHK/include/logging.ahk
  - AHK/main_dll.ahk
  - .gitignore
  - python-backend/lib/config.py
  - python-backend/lib/chunking.py
  - python-backend/lib/__init__.py
  - python-backend/lib/minimodem.py
  - python-backend/backend.py
  - python-backend/tests/test_pipeline_integration.py
  - python-backend/tests/test_loader.py
  - python-backend/tests/test_registry.py
  - python-backend/test-output-audio.py
  - python-backend/testing-audio.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "No source file (outside deleted legacy scripts) references GGWAVE_PAYLOAD_LIMIT"
    - "MODEM_PAYLOAD_LIMIT is defined in config.ahk and config.py and used consistently"
    - "Logger name 'ggwave_backend' does not appear in any Python source file"
    - "Logger name 'minimodem_backend' is used in config.py, pipeline.py, and tests"
    - "Window/dialog titles and session log headers say 'minimodem', not 'ggwave'"
    - "test-output-audio.py and testing-audio.py are deleted"
    - "Log filename in .gitignore and config.ahk is minimodem_log.txt"
  artifacts:
    - path: "AHK/include/config.ahk"
      provides: "MODEM_PAYLOAD_LIMIT constant, minimodem_log.txt path"
    - path: "python-backend/lib/config.py"
      provides: "MODEM_PAYLOAD_LIMIT constant, minimodem_backend logger"
    - path: "python-backend/tests/test_pipeline_integration.py"
      provides: "caplog targeting minimodem_backend logger"
  key_links:
    - from: "AHK/include/chunking.ahk"
      to: "AHK/include/config.ahk"
      via: "global MODEM_PAYLOAD_LIMIT"
      pattern: "MODEM_PAYLOAD_LIMIT"
    - from: "python-backend/lib/chunking.py"
      to: "python-backend/lib/config.py"
      via: "from .config import MODEM_PAYLOAD_LIMIT"
      pattern: "MODEM_PAYLOAD_LIMIT"
    - from: "python-backend/lib/pipeline.py"
      to: "python-backend/lib/config.py"
      via: "logging.getLogger('minimodem_backend')"
      pattern: "minimodem_backend"
---

<objective>
Remove all legacy ggwave naming from the active AHK and Python source. The
transport cutover to minimodem is complete; stale names in constants, logger
identifiers, window titles, and comments create confusion and traceability risk.

Purpose: Align names with the actual transport so grep, logging, and log
files all consistently say "minimodem".

Output:
- MODEM_PAYLOAD_LIMIT replaces GGWAVE_PAYLOAD_LIMIT everywhere
- Logger "minimodem_backend" replaces "ggwave_backend" everywhere
- Window/session titles updated to minimodem branding
- Two legacy ggwave test scripts deleted
- .gitignore updated for minimodem_log.txt
- Manual git submodule removal instructions for user
</objective>

<execution_context>
@C:/Users/covie/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/covie/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@E:\Git\llm-over-sound\.planning\STATE.md
@E:\Git\llm-over-sound\CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename GGWAVE_PAYLOAD_LIMIT to MODEM_PAYLOAD_LIMIT and fix AHK titles/headers</name>
  <files>AHK/include/config.ahk, AHK/include/chunking.ahk, AHK/include/gui.ahk, AHK/include/logging.ahk, AHK/main_dll.ahk, .gitignore</files>
  <action>
Make the following targeted edits — one per file:

**AHK/include/config.ahk** — three changes:
1. Rename the constant: change `GGWAVE_PAYLOAD_LIMIT := 140` to `MODEM_PAYLOAD_LIMIT := 140`. Update the inline comment to read "(dormant) Max bytes per minimodem transmission".
2. Rename the log file path: change `"ggwave_log.txt"` to `"minimodem_log.txt"` in the LOG_FILE global.
3. Update the file header comment from "Shared constants and global variables for the minimodem AHK frontend" — it is already correct on that line; verify it reads "minimodem AHK frontend" (no change needed if already so).

**AHK/include/chunking.ahk** — two changes:
1. Line 59 (in `ChunkMessageSplit`): change `global COMPRESSION_THRESHOLD, GGWAVE_PAYLOAD_LIMIT, CHUNK_DATA_SIZE` to `global COMPRESSION_THRESHOLD, MODEM_PAYLOAD_LIMIT, CHUNK_DATA_SIZE`.
2. Line 76 (in `ChunkMessageSplit`): change `StrLen(singleJson) <= GGWAVE_PAYLOAD_LIMIT` to `StrLen(singleJson) <= MODEM_PAYLOAD_LIMIT`.
(Do not alter the "skip quietly" comment — that is unrelated and intentional.)

**AHK/include/gui.ahk** — one change:
Line 99: change the Gui title from `"ggwave - Send Message"` to `"minimodem - Send Message"`.

**AHK/include/logging.ahk** — one change:
Line 15: change the session header text from `"ggwave Session Started: "` to `"minimodem Session Started: "`.

**AHK/main_dll.ahk** — one change:
Line 34: the comment currently reads `; The old ggwave protocol-id parameter is now the FSK baud rate`. Rewrite it to `; The baud rate replaces the old ggwave protocol-id parameter (FSK link parameter;` keeping the rest of the existing comment intact and coherent.

**AHK/include/chunking.ahk** — one additional change (line 352):
Change `MsgBox(... "ggwave - Message Received" ...)` to use title `"minimodem - Message Received"`.

**.gitignore** — one change:
Replace `ggwave_log.txt` with `minimodem_log.txt` on the first line. Keep `backend_log.txt` and `ggwave` (submodule entry) as-is — the bare `ggwave` entry on the last line is a directory exclusion for the submodule, leave it.

Do NOT touch AHK/main.ahk — that legacy file uses ggwave-cli over stdio and is kept as historical reference; its references are self-contained and the file is not on the active code path.
  </action>
  <verify>
    <automated>grep -rn "GGWAVE_PAYLOAD_LIMIT" "E:/Git/llm-over-sound/AHK/" && echo "FAIL: still present" || echo "PASS: not found"</automated>
  </verify>
  <done>
    - grep for GGWAVE_PAYLOAD_LIMIT in AHK/ returns no matches
    - grep for "ggwave - " (title strings) in AHK/include/ returns no matches (excluding main.ahk)
    - config.ahk LOG_FILE points to minimodem_log.txt
    - .gitignore first line is minimodem_log.txt
    - "ggwave Session Started" is gone from logging.ahk
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename MODEM_PAYLOAD_LIMIT and minimodem_backend logger in Python; delete legacy scripts</name>
  <files>python-backend/lib/config.py, python-backend/lib/chunking.py, python-backend/lib/__init__.py, python-backend/lib/minimodem.py, python-backend/backend.py, python-backend/tests/test_pipeline_integration.py, python-backend/tests/test_loader.py, python-backend/tests/test_registry.py, python-backend/test-output-audio.py, python-backend/testing-audio.py</files>
  <action>
**python-backend/lib/config.py** — four changes:
1. Module docstring first line: change `"Shared configuration constants and logging setup for the ggwave backend."` to `"Shared configuration constants and logging setup for the minimodem backend."`.
2. Rename constant: change `GGWAVE_PAYLOAD_LIMIT = 140` to `MODEM_PAYLOAD_LIMIT = 140`. Update the inline comment to `# Max bytes per minimodem transmission (dormant chunking path)`.
3. Logger name: change `logging.getLogger("ggwave_backend")` to `logging.getLogger("minimodem_backend")`.
4. Session start log string: change `"ggwave Backend Session Started"` to `"minimodem Backend Session Started"`.

**python-backend/lib/chunking.py** — one change:
The module docstring already says "minimodem" throughout (confirmed in read). Locate the import line: `from .config import GGWAVE_PAYLOAD_LIMIT` (or wherever GGWAVE_PAYLOAD_LIMIT is imported/used) and rename to `MODEM_PAYLOAD_LIMIT`. Also rename all usages of `GGWAVE_PAYLOAD_LIMIT` within the file body to `MODEM_PAYLOAD_LIMIT`.

**python-backend/lib/__init__.py** — one change:
Module docstring: change `"lib — shared modules for the ggwave radiology backend."` to `"lib — shared modules for the minimodem radiology backend."`.

**python-backend/lib/minimodem.py** — one change:
Line 4 comment: change `"This replaces the ggwave Python binding in the transport path."` to `"This is the minimodem FSK transport binding (replaced the old ggwave Python binding)."`.

**python-backend/backend.py** — one change:
Line 10 docstring: the phrase `"transport swapped from the old ggwave/PyAudio stack to the minimodem"` is fine as historical context — change it to `"transport uses the minimodem ctypes binding (lib/minimodem.py -> libminimodem_simple.so); the old ggwave/PyAudio stack was replaced in Phase 7."` so it is past-tense and contextually accurate.

**python-backend/tests/test_pipeline_integration.py** — one change:
Line 160: change `caplog.at_level(logging.INFO, logger="ggwave_backend")` to `caplog.at_level(logging.INFO, logger="minimodem_backend")`.

**python-backend/tests/test_loader.py** — one change:
The docstring on the standalone import test at line 136: change `"lib.templates module tree does not import ggwave, pyaudio, or audio."` — update to `"lib.templates module tree does not import ggwave, pyaudio, or audio (verifies Phase 7 transport isolation)."`. Do NOT change the assertion logic — `("ggwave", "pyaudio", "lib.audio", ".audio")` are the forbidden import patterns and must remain exactly as-is. These tests verify migration correctness.

**python-backend/tests/test_registry.py** — one change:
The docstring at line 178: change `"FWRK-01: Template sub-package source has no ggwave/pyaudio imports."` to `"FWRK-01: Template sub-package source has no ggwave/pyaudio imports (Phase 7 migration guard)."`. Update the comment at line 181 from `"since other test modules may load ggwave as a side effect"` to `"since other test modules may have loaded ggwave-era modules as a side effect."`. Do NOT change the assertion logic — the `"import ggwave"` string literal in assert statements is the detection pattern, not a code import, and must remain.

**Delete python-backend/test-output-audio.py**: This file imports `ggwave` and `pyaudio` directly and is a legacy test script from before the transport cutover. Delete it.

**Delete python-backend/testing-audio.py**: This file imports `pyaudio` and is a legacy PyAudio device listing script. Delete it.

Note on test_loader.py / test_registry.py: The string literals `"import ggwave"` in those test assertions are detection patterns (checked via `inspect.getsource`), not imports — do not alter them. They verify that production modules remain ggwave-free.
  </action>
  <verify>
    <automated>grep -rn "ggwave_backend" "E:/Git/llm-over-sound/python-backend/" && echo "FAIL: still present" || echo "PASS: not found"</automated>
  </verify>
  <done>
    - grep for "ggwave_backend" in python-backend/ returns no matches
    - grep for GGWAVE_PAYLOAD_LIMIT in python-backend/ returns no matches
    - test-output-audio.py and testing-audio.py do not exist
    - pytest python-backend/tests/ passes (run from python-backend/ with PIPELINE_MODE=test)
    - "minimodem Backend Session Started" appears in config.py log_session_start()
  </done>
</task>

<task type="auto">
  <name>Task 3: Document git submodule removal instructions (user manual step)</name>
  <files>documentation/retire-legacy-submodules.md</files>
  <action>
Create the file `documentation/retire-legacy-submodules.md` with the following content
(prose only — no code fences needed inside action; write the file directly):

The file should contain:

Title: "Retiring Legacy ggwave and quiet Git Submodules"

Section "Background": Explain that `ggwave`, `quiet`, and `minimodem` are git submodules
(mode 160000 in the index). There is no `.gitmodules` file — the entries exist only in the
git index. The transport is now minimodem; ggwave and quiet are no longer needed.
`minimodem` must be kept.

Section "Why This Is a Manual Step": Git submodule removal is a destructive index
operation that rewrites history-adjacent index state. It must be done consciously by the
developer. Claude does not run destructive git commands without explicit instruction.

Section "Commands to run (once)": Provide the exact shell commands:
  git rm ggwave
  git rm quiet
  git commit -m "chore: remove retired ggwave and quiet submodules"

Explain that `git rm` on a submodule removes the index entry and the working-tree
directory. The actual upstream repos are not affected. After committing, the directories
will no longer appear in `git status` as untracked submodule entries.

Section "After removal": Note that the bare `ggwave` line in `.gitignore` was there to
suppress the submodule directory from appearing as untracked. After `git rm ggwave`, that
entry in `.gitignore` can also be removed, but it is harmless to leave it.

Section "minimodem": Note that `minimodem` stays as-is — it is the active transport
submodule and must not be removed.

Keep the file concise (under 40 lines). Do not include a build checklist — this is
a one-time operator runbook, not a development doc.
  </action>
  <verify>
    <automated>test -f "E:/Git/llm-over-sound/documentation/retire-legacy-submodules.md" && echo "PASS: file exists" || echo "FAIL: file missing"</automated>
  </verify>
  <done>
    - documentation/retire-legacy-submodules.md exists
    - It contains the two `git rm` commands and the `git commit` command verbatim
    - It explains why this is a manual step
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source rename scope | Renaming string literals — risk of partial rename leaving inconsistent names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-wf6-01 | Tampering | test_loader.py / test_registry.py assert literals | accept | The `"import ggwave"` string in assertions is a detection pattern, not an import. Verified by reading the test logic — intentionally preserved. |
| T-wf6-02 | Repudiation | Logger name change | mitigate | Logger rename is atomic in config.py; pipeline.py and tests updated in same task to stay consistent. |
</threat_model>

<verification>
After both source-code tasks complete:

1. `grep -rn "GGWAVE_PAYLOAD_LIMIT" AHK/ python-backend/lib/` — must return zero matches.
2. `grep -rn "ggwave_backend" python-backend/` — must return zero matches.
3. `grep -n "ggwave - " AHK/include/` — must return zero matches (ignoring AHK/main.ahk).
4. `grep -n "minimodem_log.txt" AHK/include/config.ahk` — must return one match.
5. `grep -n "minimodem_log.txt" .gitignore` — must return one match.
6. `ls python-backend/test-output-audio.py python-backend/testing-audio.py 2>&1` — both must report "No such file".
7. `ls documentation/retire-legacy-submodules.md` — must exist.
8. From `python-backend/`: `PIPELINE_MODE=test python -m pytest tests/ -q` — all tests pass.
</verification>

<success_criteria>
- Zero occurrences of `GGWAVE_PAYLOAD_LIMIT` in AHK/ or python-backend/lib/ (excluding deleted files)
- Zero occurrences of `"ggwave_backend"` anywhere in python-backend/
- Window titles, session headers, and module docstrings all say "minimodem"
- Log filename is minimodem_log.txt in both config.ahk and .gitignore
- Legacy scripts test-output-audio.py and testing-audio.py are deleted
- Test suite passes with PIPELINE_MODE=test
- documentation/retire-legacy-submodules.md exists with the two git rm commands
</success_criteria>

<output>
Create `.planning/quick/260626-wf6-remove-ggwave-quiet-references-from-ahk-/260626-wf6-SUMMARY.md` when done.
</output>
