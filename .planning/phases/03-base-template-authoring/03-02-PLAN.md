---
phase: 03-base-template-authoring
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - python-backend/rpt_templates/ct/ct_ap_structured.rpt.md
  - python-backend/rpt_templates/ct/ct_thorax.rpt.md
  - python-backend/rpt_templates/us/us_hbs.rpt.md
autonomous: true
requirements: [SMPL-01, SMPL-02, SMPL-04, FLDS-02]
must_haves:
  truths:
    - "CT AP structured variant loads and validates with different alias set from freeform"
    - "CT thorax template loads and validates with 8 fields including 2 optional"
    - "US HBS template loads and validates with measurement placeholders in body"
    - "All 4 production templates load without error through discover_templates"
    - "No alias collision between any production templates"
  artifacts:
    - path: "python-backend/rpt_templates/ct/ct_ap_structured.rpt.md"
      provides: "CT AP structured variant with tabular organ list layout"
      contains: "study_name: \"CT Abdomen and Pelvis (Structured)\""
    - path: "python-backend/rpt_templates/ct/ct_thorax.rpt.md"
      provides: "CT thorax freeform template"
      contains: "study_name: \"CT Thorax\""
    - path: "python-backend/rpt_templates/us/us_hbs.rpt.md"
      provides: "US HBS template with measurement placeholders"
      contains: "measurement:liver_span_cm"
  key_links:
    - from: "python-backend/rpt_templates/ct/ct_ap_structured.rpt.md"
      to: "python-backend/lib/templates/schema.py"
      via: "TemplateSchema validation at load time"
      pattern: "TemplateSchema"
    - from: "python-backend/rpt_templates/us/us_hbs.rpt.md"
      to: "python-backend/lib/templates/schema.py"
      via: "validate_body_placeholders cross-checks measurement: typed placeholders"
      pattern: "measurement:"
---

<objective>
Author the remaining 3 production templates: CT AP structured variant, CT thorax freeform, and US HBS freeform. All templates validate against the existing schema and pass the integration tests created in Plan 01.

Purpose: Completes the Phase 3 deliverable of 4 production templates (3 study types, 1 variant). Proves the variant pattern with structured CT AP, establishes CT thorax as a second modality template, and proves measurement placeholders work with US HBS.

Output: ct_ap_structured.rpt.md, ct_thorax.rpt.md, us_hbs.rpt.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-base-template-authoring/03-CONTEXT.md
@.planning/phases/03-base-template-authoring/03-RESEARCH.md
@.planning/phases/03-base-template-authoring/03-01-SUMMARY.md

@python-backend/lib/templates/schema.py
@python-backend/lib/templates/loader.py
@python-backend/rpt_templates/ct/ct_ap.rpt.md
@python-backend/tests/test_production_templates.py

<interfaces>
<!-- Key types from schema.py (after Plan 01 update) -->

From python-backend/lib/templates/schema.py:
```python
class FieldDefinition(BaseModel):
    model_config = ConfigDict(extra="forbid")
    name: str
    normal: str
    sex: Optional[str] = None
    optional: bool = False  # Added in Plan 01

class TemplateSchema(BaseModel):
    model_config = ConfigDict(extra="forbid")
    study_name: str
    aliases: list[str]
    fields: list[FieldDefinition]
    groups: list[FieldGroup] = []
    technique: str
    interpolate_normal: bool = False
    impression: bool = True
    important_first: bool = False
```

From python-backend/lib/templates/loader.py:
```python
def load_template(path: pathlib.Path) -> LoadedTemplate:
def discover_templates(templates_dir: pathlib.Path) -> list[pathlib.Path]:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author CT AP structured variant and CT thorax template</name>
  <files>python-backend/rpt_templates/ct/ct_ap_structured.rpt.md, python-backend/rpt_templates/ct/ct_thorax.rpt.md</files>
  <read_first>
    - python-backend/rpt_templates/ct/ct_ap.rpt.md (reference for field list -- structured variant uses same fields per D-23/D-26)
    - python-backend/tests/test_production_templates.py (tests these templates must pass)
    - .planning/phases/03-base-template-authoring/03-CONTEXT.md (D-09 CT thorax fields, D-19-26 variant decisions)
    - .planning/phases/03-base-template-authoring/03-RESEARCH.md (CT Thorax Field Inventory, Structured Variant Body Layout)
  </read_first>
  <action>
**File 1: `python-backend/rpt_templates/ct/ct_ap_structured.rpt.md`**

Per D-20, D-21, D-24, D-26. Same 18 fields and 2 groups as the freeform variant (copy from ct_ap.rpt.md frontmatter). Different body layout and alias set. Sets `important_first: true` per research recommendation.

YAML Frontmatter fields:
- study_name: "CT Abdomen and Pelvis (Structured)"
- aliases: ["ct ap structured", "ct abdomen structured", "ct abdomen pelvis structured", "ct abdomen and pelvis structured"]
- technique: "CT of the abdomen and pelvis was performed with intravenous contrast in the portal venous phase."
- interpolate_normal: false
- impression: true
- important_first: true (different from freeform)
- fields: SAME 18 fields as ct_ap.rpt.md (liver through soft_tissues, same order, same normal text, same sex/optional flags)
- groups: SAME 2 groups as ct_ap.rpt.md (gallbladder_cbd and spleen_adrenals_pancreas with same partials)

CRITICAL: aliases must NOT overlap with ct_ap.rpt.md aliases. Freeform owns "ct ap", structured uses "ct ap structured" suffix per D-22.

Body layout (structured tabular per D-20, D-24):
- `## CLINICAL HISTORY` with `{{technique:clinical_indication}}`
- `## TECHNIQUE` with `{{technique:phase}}`
- `## COMPARISON` with "None available."
- `## FINDINGS` containing a markdown table with columns "Organ" and "Status", one row per field:
  ```
  | Organ | Status |
  |-------|--------|
  | Liver | {{liver}} |
  | Gallbladder | {{gallbladder}} |
  | CBD | {{cbd}} |
  | Spleen | {{spleen}} |
  | Adrenals | {{adrenals}} |
  | Pancreas | {{pancreas}} |
  | Kidneys | {{kidneys}} |
  | Bowel | {{bowel}} |
  | Mesentery | {{mesentery}} |
  | Lymph nodes | {{lymph_nodes}} |
  | Bladder | {{bladder}} |
  | Uterus/Ovaries | {{uterus_ovaries}} |
  | Prostate | {{prostate}} |
  | Vessels | {{vessels}} |
  | Lung bases | {{lung_bases}} |
  | Free fluid | {{free_fluid}} |
  | Bones | {{bones}} |
  | Soft tissues | {{soft_tissues}} |
  ```
- `### Key Findings` subsection with placeholder text "(Clinically significant findings)"
- `### Other Findings` subsection with placeholder text "(Incidental or minor findings)"
- `## Guidance` with same 3 guidance lines as the freeform variant (aorta calibre, CBD calibre, Bosniak/LI-RADS)
- `## COMMENT`

**File 2: `python-backend/rpt_templates/ct/ct_thorax.rpt.md`**

Per D-09, D-10, D-11, D-12. 8 fields, 2 optional (airways, thyroid). No groups. No measurements. No sex-dependent fields.

YAML Frontmatter fields:
- study_name: "CT Thorax"
- aliases: ["ct thorax", "ct chest", "ct lung"]
- technique: "CT of the thorax was performed with intravenous contrast in the arterial phase."
- interpolate_normal: false
- impression: true
- important_first: false
- groups: [] (empty list)

Fields list (8 fields in this exact order per D-09):
1. lungs: normal="The lungs are clear. No consolidation, mass or nodule."
2. pleura: normal="No pleural effusion or pneumothorax."
3. airways: optional=true, normal="The trachea and main bronchi are patent."
4. thyroid: optional=true, normal="The thyroid gland is unremarkable."
5. mediastinum: normal="No pathological mediastinal or hilar lymphadenopathy."
6. heart_great_vessels: normal="The heart is normal in size. No pericardial effusion. The thoracic aorta is normal in calibre."
7. limited_abdomen: normal="The visualised upper abdomen is unremarkable."
8. bones: normal="No suspicious osseous lesion."

Body (freeform per D-19): Same section structure as CT AP freeform -- CLINICAL HISTORY, TECHNIQUE, COMPARISON, FINDINGS (8 field placeholders, one per line), Guidance, COMMENT.

Guidance content (3 lines):
- "Lymph node short axis threshold: 10 mm for pathological mediastinal or hilar lymphadenopathy."
- "Fleischner Society guidelines for incidental pulmonary nodule management."
- "Normal thoracic aortic calibre: up to 4.0 cm at mid-ascending aorta."
  </action>
  <verify>
    <automated>cd python-backend && python -m pytest tests/test_production_templates.py::test_ct_ap_structured_loads tests/test_production_templates.py::test_ct_thorax_loads tests/test_production_templates.py::test_ct_thorax_optional_fields -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists at python-backend/rpt_templates/ct/ct_ap_structured.rpt.md
    - ct_ap_structured.rpt.md contains `study_name: "CT Abdomen and Pelvis (Structured)"`
    - ct_ap_structured.rpt.md aliases include "ct ap structured" but NOT "ct ap"
    - ct_ap_structured.rpt.md body contains `| Organ | Status |` markdown table header
    - ct_ap_structured.rpt.md body contains `### Key Findings` and `### Other Findings` sections
    - ct_ap_structured.rpt.md sets `important_first: true`
    - File exists at python-backend/rpt_templates/ct/ct_thorax.rpt.md
    - ct_thorax.rpt.md contains exactly 8 fields in fields list
    - ct_thorax.rpt.md fields `airways` and `thyroid` have `optional: true`
    - ct_thorax.rpt.md contains `## Guidance` with "Fleischner" reference
    - test_ct_ap_structured_loads, test_ct_thorax_loads, test_ct_thorax_optional_fields all pass
  </acceptance_criteria>
  <done>CT AP structured variant and CT thorax templates load and validate, pass all integration tests, no alias collision with existing templates</done>
</task>

<task type="auto">
  <name>Task 2: Author US HBS template and run full validation</name>
  <files>python-backend/rpt_templates/us/us_hbs.rpt.md</files>
  <read_first>
    - python-backend/tests/test_production_templates.py (tests this template must pass)
    - python-backend/rpt_templates/ct/ct_ap.rpt.md (reference for YAML structure pattern)
    - .planning/phases/03-base-template-authoring/03-CONTEXT.md (D-13-14 US HBS fields, D-27-31 measurements)
    - .planning/phases/03-base-template-authoring/03-RESEARCH.md (US HBS Field Inventory, US HBS Guidance)
  </read_first>
  <action>
Create `python-backend/rpt_templates/us/us_hbs.rpt.md`. Must create the `us/` subdirectory first.

Per D-13, D-14, D-27, D-28, D-29, D-31. 5 fields. 3 measurement placeholders embedded in normal text prose. `others` is optional with empty normal text. No groups. No sex-dependent fields.

YAML Frontmatter fields:
- study_name: "US Hepatobiliary"
- aliases: ["us hbs", "us hepatobiliary", "ultrasound hbs", "ultrasound hepatobiliary"]
- technique: "Ultrasound of the hepatobiliary system was performed."
- interpolate_normal: false
- impression: true
- important_first: false
- groups: [] (empty list)

Fields list (5 fields in this exact order per D-13):
1. liver: normal="The liver is normal in echotexture, measuring {{measurement:liver_span_cm}} cm in craniocaudal span. No focal lesion."
2. gallbladder_cbd: normal="The gallbladder is normal with no gallstones or wall thickening. The common bile duct measures {{measurement:cbd_diameter_mm}} mm, within normal limits."
3. spleen: normal="The spleen is normal in echotexture, measuring {{measurement:spleen_length_cm}} cm."
4. pancreas: normal="The pancreas is normal in echotexture where visualised."
5. others: optional=true, normal="" (empty string)

CRITICAL per D-28: Measurement placeholders use `{{measurement:name}}` syntax with the `measurement:` type prefix. NOT `{{liver_span_cm}}` (that would be treated as a plain field placeholder and fail validation).

Body (freeform per D-19): CLINICAL HISTORY with `{{technique:clinical_indication}}`, TECHNIQUE with `{{technique:phase}}`, COMPARISON with "None available.", FINDINGS with 5 field placeholders (one per line), Guidance, COMMENT.

Guidance content (3 lines):
- "Normal liver span: up to 15.5 cm (craniocaudal)."
- "Normal CBD calibre: up to 6 mm (up to 10 mm post-cholecystectomy)."
- "Normal spleen length: up to 12 cm."

After creating the template, run the full test suite to confirm:
1. All production template tests pass (including test_all_production_templates_load which expects exactly 4 files)
2. No alias collisions across all 4 templates
3. Existing Phase 1/2 tests still pass
  </action>
  <verify>
    <automated>cd python-backend && python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists at python-backend/rpt_templates/us/us_hbs.rpt.md
    - us_hbs.rpt.md contains `study_name: "US Hepatobiliary"`
    - us_hbs.rpt.md contains exactly 5 fields in fields list
    - us_hbs.rpt.md field `others` has `optional: true` and `normal: ""`
    - us_hbs.rpt.md body contains the strings `{{measurement:liver_span_cm}}`, `{{measurement:cbd_diameter_mm}}`, `{{measurement:spleen_length_cm}}`
    - us_hbs.rpt.md contains `## Guidance` with "15.5 cm" liver span threshold
    - test_us_hbs_loads, test_us_hbs_measurements, test_us_hbs_optional_fields all pass
    - test_all_production_templates_load passes (discovers exactly 4 templates)
    - Full test suite passes with 0 failures
  </acceptance_criteria>
  <done>US HBS template loads and validates with measurement placeholders, all 4 production templates discoverable, full test suite green</done>
</task>

</tasks>

<verification>
1. `cd python-backend && python -m pytest tests/test_production_templates.py -v` -- all 13 tests pass
2. `cd python-backend && python -m pytest tests/ -v` -- full suite green, no regressions
3. `cd python-backend && python -c "from lib.templates.loader import discover_templates; import pathlib; print(len(discover_templates(pathlib.Path('rpt_templates'))))"` -- prints 4
4. Manual check: no alias appears in more than one template file
</verification>

<success_criteria>
- 4 production templates exist in rpt_templates/ (ct_ap, ct_ap_structured, ct_thorax, us_hbs)
- CT AP structured variant uses tabular body layout with Key/Other Findings sections
- CT thorax has 8 fields with airways and thyroid optional
- US HBS has 3 measurement placeholders embedded in normal text prose
- All production template tests pass
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/03-base-template-authoring/03-02-SUMMARY.md`
</output>
