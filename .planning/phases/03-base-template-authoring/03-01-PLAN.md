---
phase: 03-base-template-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python-backend/lib/templates/schema.py
  - python-backend/tests/test_production_templates.py
  - python-backend/rpt_templates/ct/ct_ap.rpt.md
autonomous: true
requirements: [SMPL-01, FLDS-01]
must_haves:
  truths:
    - "FieldDefinition accepts optional=True without validation error"
    - "CT AP freeform template loads and validates through the existing loader"
    - "CT AP template has 18 fields in craniocaudal order with sex-dependent pelvis fields"
    - "CT AP template has 2 field groups with joint normal text and partials"
    - "CT AP template has a guidance section with real clinical thresholds"
  artifacts:
    - path: "python-backend/lib/templates/schema.py"
      provides: "FieldDefinition with optional flag"
      contains: "optional: bool = False"
    - path: "python-backend/rpt_templates/ct/ct_ap.rpt.md"
      provides: "CT AP freeform production template"
      contains: "study_name: \"CT Abdomen and Pelvis\""
    - path: "python-backend/tests/test_production_templates.py"
      provides: "Integration tests for all production templates"
      contains: "test_ct_ap_loads"
  key_links:
    - from: "python-backend/rpt_templates/ct/ct_ap.rpt.md"
      to: "python-backend/lib/templates/schema.py"
      via: "TemplateSchema Pydantic validation at load time"
      pattern: "TemplateSchema\\(\\*\\*"
    - from: "python-backend/tests/test_production_templates.py"
      to: "python-backend/lib/templates/loader.py"
      via: "load_template() call"
      pattern: "load_template"
---

<objective>
Add the `optional` flag to FieldDefinition (D-38 pre-phase blocker), author the CT abdomen/pelvis freeform template with full clinical content, and create the integration test scaffold that validates all production templates.

Purpose: Establishes the largest and most complex template (CT AP with 18 fields, 2 groups, sex-dependent pelvis, guidance section) as the reference pattern for remaining templates. The schema update unblocks all templates that use optional fields.

Output: Updated schema.py, ct_ap.rpt.md production template, test_production_templates.py test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-base-template-authoring/03-CONTEXT.md
@.planning/phases/03-base-template-authoring/03-RESEARCH.md

@python-backend/lib/templates/schema.py
@python-backend/lib/templates/loader.py
@python-backend/tests/fixtures/sample_template.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From python-backend/lib/templates/schema.py:
```python
class FieldDefinition(BaseModel):
    model_config = ConfigDict(extra="forbid")
    name: str
    normal: str
    sex: Optional[str] = None
    # NOTE: `optional` field does NOT exist yet -- Task 1 adds it

class FieldGroup(BaseModel):
    model_config = ConfigDict(extra="forbid")
    name: str
    members: list[str]
    joint_normal: str
    partials: list[GroupPartial] = []

class TemplateSchema(BaseModel):
    model_config = ConfigDict(extra="forbid")
    study_name: str
    aliases: list[str]
    fields: list[FieldDefinition]
    groups: list[FieldGroup] = []
    technique: str
    interpolate_normal: bool = False
    impression: bool = True
    important_first: bool = False
```

From python-backend/lib/templates/loader.py:
```python
@dataclass(frozen=True)
class LoadedTemplate:
    schema: TemplateSchema
    body: str
    file_path: str

def load_template(path: pathlib.Path) -> LoadedTemplate:
def discover_templates(templates_dir: pathlib.Path) -> list[pathlib.Path]:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional flag to FieldDefinition and create test scaffold</name>
  <files>python-backend/lib/templates/schema.py, python-backend/tests/test_production_templates.py</files>
  <read_first>
    - python-backend/lib/templates/schema.py (current FieldDefinition class)
    - python-backend/lib/templates/loader.py (load_template function signature)
    - python-backend/tests/fixtures/sample_template.md (reference template format)
  </read_first>
  <action>
1. In `python-backend/lib/templates/schema.py`, add `optional: bool = False` to the `FieldDefinition` class, after the `sex` field. The full field list becomes:
   ```python
   name: str
   normal: str
   sex: Optional[str] = None
   optional: bool = False
   ```
   No other changes to schema.py. Per D-38.

2. Create `python-backend/tests/test_production_templates.py` with the following test functions. All tests import `load_template` from `lib.templates.loader` and use `pathlib.Path`. The production templates root is `pathlib.Path(__file__).parent.parent / "rpt_templates"`.

   Test functions to create (all will initially fail since templates don't exist yet -- that is expected):

   **test_ct_ap_loads**: Load `rpt_templates/ct/ct_ap.rpt.md` via `load_template()`. Assert no exception. Assert `schema.study_name == "CT Abdomen and Pelvis"`. Assert `len(schema.fields) == 18`. Assert `"ct ap" in schema.aliases`.

   **test_ct_ap_field_order**: Load CT AP. Extract field names as list: `[f.name for f in schema.fields]`. Assert the list equals exactly: `["liver", "gallbladder", "cbd", "spleen", "adrenals", "pancreas", "kidneys", "bowel", "mesentery", "lymph_nodes", "bladder", "uterus_ovaries", "prostate", "vessels", "lung_bases", "free_fluid", "bones", "soft_tissues"]`.

   **test_ct_ap_sex_fields** (FLDS-01): Load CT AP. Find field where `name == "uterus_ovaries"`, assert `sex == "female"`. Find field where `name == "prostate"`, assert `sex == "male"`. Assert no other fields have a sex tag.

   **test_ct_ap_optional_fields**: Load CT AP. Find field `vessels`, assert `optional is True`. Assert all other fields have `optional is False`.

   **test_ct_ap_groups**: Load CT AP. Assert `len(schema.groups) == 2`. Find group `gallbladder_cbd`, assert members == `["gallbladder", "cbd"]`. Find group `spleen_adrenals_pancreas`, assert members == `["spleen", "adrenals", "pancreas"]` and `len(partials) == 3`.

   **test_ct_ap_guidance**: Load CT AP. Assert `"## Guidance"` in body. Assert `"Bosniak"` in body (renal cyst classification reference). Assert `"aorta"` in body guidance section.

   **test_ct_ap_structured_loads**: Load `rpt_templates/ct/ct_ap_structured.rpt.md`. Assert `schema.study_name == "CT Abdomen and Pelvis (Structured)"`. Assert `"ct ap structured" in schema.aliases`. Assert `"ct ap" not in schema.aliases` (no alias collision with freeform).

   **test_ct_thorax_loads**: Load `rpt_templates/ct/ct_thorax.rpt.md`. Assert `schema.study_name == "CT Thorax"`. Assert `len(schema.fields) == 8`. Assert field names equal: `["lungs", "pleura", "airways", "thyroid", "mediastinum", "heart_great_vessels", "limited_abdomen", "bones"]`.

   **test_ct_thorax_optional_fields**: Load CT thorax. Assert `airways` and `thyroid` are optional. All others not optional.

   **test_us_hbs_loads**: Load `rpt_templates/us/us_hbs.rpt.md`. Assert `schema.study_name == "US Hepatobiliary"`. Assert `len(schema.fields) == 5`. Assert `"us hbs" in schema.aliases`.

   **test_us_hbs_measurements** (FLDS-02): Load US HBS. Assert body contains `"{{measurement:liver_span_cm}}"`, `"{{measurement:cbd_diameter_mm}}"`, `"{{measurement:spleen_length_cm}}"`.

   **test_us_hbs_optional_fields**: Load US HBS. Assert `others` field is optional. All others not optional.

   **test_all_production_templates_load**: Use `discover_templates()` on the production root. Assert `len(paths) == 4` (ct_ap, ct_ap_structured, ct_thorax, us_hbs). Load each via `load_template()`. Assert no exceptions.

   Use `pytest.mark.skipif` with `not path.exists()` on tests for templates not yet authored (ct_ap_structured, ct_thorax, us_hbs) so the test file is valid even before Plan 02 runs. The CT AP tests should NOT be skipped (they run after Task 2 of this plan).
  </action>
  <verify>
    <automated>cd python-backend && python -m pytest tests/ -x -q --ignore=tests/test_production_templates.py</automated>
  </verify>
  <acceptance_criteria>
    - schema.py contains `optional: bool = False` inside class FieldDefinition
    - `python -c "from lib.templates.schema import FieldDefinition; FieldDefinition(name='test', normal='test', optional=True)"` succeeds without error
    - test_production_templates.py contains all 13 test functions listed above
    - test_production_templates.py imports load_template and discover_templates from lib.templates.loader
    - Existing test suite (excluding production template tests) passes with 0 failures
  </acceptance_criteria>
  <done>FieldDefinition accepts optional=True, test scaffold exists with 13 tests covering all 5 requirements, existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Author CT abdomen/pelvis freeform template</name>
  <files>python-backend/rpt_templates/ct/ct_ap.rpt.md</files>
  <read_first>
    - python-backend/lib/templates/schema.py (updated FieldDefinition with optional flag)
    - python-backend/tests/fixtures/sample_template.md (reference YAML+body format)
    - python-backend/tests/test_production_templates.py (tests this template must pass)
    - .planning/phases/03-base-template-authoring/03-CONTEXT.md (D-02 field list, D-04 sex fields, D-05 vessels optional, D-15-18 normal text style, D-35-37 guidance)
    - .planning/phases/03-base-template-authoring/03-RESEARCH.md (CT AP Field Inventory table with normal text, groups, guidance content)
  </read_first>
  <action>
Create `python-backend/rpt_templates/ct/ct_ap.rpt.md`. Must create the `ct/` subdirectory first.

The file is a YAML frontmatter + markdown body template. The YAML frontmatter is delimited by `---` lines at the top and bottom.

**YAML Frontmatter fields** (copy these values exactly):

- study_name: "CT Abdomen and Pelvis"
- aliases: ["ct ap", "ct abdomen", "ct abdomen pelvis", "ct abdomen and pelvis"]
- technique: "CT of the abdomen and pelvis was performed with intravenous contrast in the portal venous phase."
- interpolate_normal: false
- impression: true
- important_first: false

**Fields list** (18 fields in this exact order, per D-02):

1. liver: normal="The liver is normal in size and attenuation. No focal lesion."
2. gallbladder: normal="The gallbladder is normal. No gallstones."
3. cbd: normal="The common bile duct is normal in calibre."
4. spleen: normal="The spleen is normal in size."
5. adrenals: normal="The adrenal glands are normal."
6. pancreas: normal="The pancreas is normal in morphology and enhancement."
7. kidneys: normal="The kidneys are normal in size and enhancement. No hydronephrosis or renal calculus."
8. bowel: normal="The visualised bowel is unremarkable. No dilatation or wall thickening."
9. mesentery: normal="The mesentery is clear."
10. lymph_nodes: normal="No pathologically enlarged lymph nodes."
11. bladder: normal="The urinary bladder is normal."
12. uterus_ovaries: sex="female", normal="The uterus and ovaries are normal in size and morphology."
13. prostate: sex="male", normal="The prostate is normal in size."
14. vessels: optional=true, normal="The abdominal aorta is normal in calibre. The IVC and portal vein are patent."
15. lung_bases: normal="The visualised lung bases are clear."
16. free_fluid: normal="No free fluid."
17. bones: normal="No suspicious osseous lesion."
18. soft_tissues: normal="The abdominal wall and soft tissues are unremarkable."

**Groups** (2 groups):

1. gallbladder_cbd: members=[gallbladder, cbd], joint_normal="The gallbladder and common bile duct are normal. No gallstones.", partials=[]
2. spleen_adrenals_pancreas: members=[spleen, adrenals, pancreas], joint_normal="The spleen, adrenal glands and pancreas are unremarkable.", partials with all 3 two-member combinations:
   - [spleen, adrenals] -> "The spleen and adrenal glands are unremarkable."
   - [spleen, pancreas] -> "The spleen and pancreas are unremarkable."
   - [adrenals, pancreas] -> "The adrenal glands and pancreas are unremarkable."

**Markdown Body** (freeform continuous prose, one `{{field_name}}` placeholder per line):

Sections in order: `## CLINICAL HISTORY` with `{{technique:clinical_indication}}`, `## TECHNIQUE` with `{{technique:phase}}`, `## COMPARISON` with "None available.", `## FINDINGS` with all 18 field placeholders in craniocaudal order (one per line, blank line between each), `## Guidance` with 3 lines of clinical thresholds, `## COMMENT` at the end.

Guidance content (3 lines):
- "Normal aortic calibre: infrarenal aorta up to 2.0 cm (men), 1.8 cm (women). Aneurysm threshold: 3.0 cm."
- "Normal CBD calibre: up to 6 mm (up to 10 mm post-cholecystectomy)."
- "Bosniak classification for renal cysts. LI-RADS for liver lesions on contrast-enhanced CT."

Per D-02 field order, D-04 sex fields, D-05 vessels optional, D-06 lymph_nodes, D-07 bowel/mesentery separate, D-15-17 normal text style, D-35-37 guidance content.
  </action>
  <verify>
    <automated>cd python-backend && python -m pytest tests/test_production_templates.py::test_ct_ap_loads tests/test_production_templates.py::test_ct_ap_field_order tests/test_production_templates.py::test_ct_ap_sex_fields tests/test_production_templates.py::test_ct_ap_optional_fields tests/test_production_templates.py::test_ct_ap_groups tests/test_production_templates.py::test_ct_ap_guidance -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File exists at python-backend/rpt_templates/ct/ct_ap.rpt.md
    - YAML frontmatter contains `study_name: "CT Abdomen and Pelvis"`
    - YAML frontmatter contains exactly 18 fields in the fields list
    - Field `uterus_ovaries` has `sex: "female"`, field `prostate` has `sex: "male"`
    - Field `vessels` has `optional: true`
    - Groups list contains `gallbladder_cbd` and `spleen_adrenals_pancreas`
    - Body contains `## Guidance` section with "Bosniak" and "aorta" references
    - All 6 CT AP test functions pass
  </acceptance_criteria>
  <done>CT AP freeform template loads and validates, passes all CT AP tests including field order, sex fields, optional fields, groups, and guidance checks</done>
</task>

</tasks>

<verification>
1. `cd python-backend && python -m pytest tests/ -x -q --ignore=tests/test_production_templates.py` -- existing suite still passes
2. `cd python-backend && python -m pytest tests/test_production_templates.py -k "ct_ap" -v` -- all CT AP tests pass
3. `cd python-backend && python -c "from lib.templates.loader import load_template; import pathlib; t = load_template(pathlib.Path('rpt_templates/ct/ct_ap.rpt.md')); print(f'{t.schema.study_name}: {len(t.schema.fields)} fields')"` -- prints "CT Abdomen and Pelvis: 18 fields"
</verification>

<success_criteria>
- FieldDefinition schema supports optional=True without breaking existing tests
- CT AP freeform template exists at rpt_templates/ct/ct_ap.rpt.md with 18 fields, 2 groups, sex-dependent pelvis, guidance section
- Production template test scaffold exists with tests for all 4 templates (Plan 02 templates skipped via skipif)
- All CT AP-specific tests pass green
</success_criteria>

<output>
After completion, create `.planning/phases/03-base-template-authoring/03-01-SUMMARY.md`
</output>
