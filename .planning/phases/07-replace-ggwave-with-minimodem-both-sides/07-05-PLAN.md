---
phase: 07-replace-ggwave-with-minimodem-both-sides
plan: 05
type: execute
wave: 4
depends_on: ["07-03", "07-04"]
files_modified:
  - python-backend/lib/minimodem.py
  - AHK/ggwave_simple.dll
  - AHK/SDL2.dll
autonomous: false
requirements: []
must_haves:
  truths:
    - "An AHK frontend and a Pi Python backend complete a cross-machine round trip over the USB audio cable: a draft is sent, the formatted report returns intact, both ends at matching baud"
    - "A ~500-byte report-length single-frame payload survives the link; an induced corruption causes a CRC mismatch that triggers full-message retransmit and never surfaces a partial/corrupt report"
    - "A baud sweep (1200/4800/9600) without recompile confirms both ends must match; the baud at which the interface's passband fails is recorded"
    - "minimodem_simple.dll has zero non-system dependencies (verified by dependency walker) and the retired ggwave_simple.dll + SDL2.dll are removed from AHK/ after the round trip is confirmed"
  artifacts:
    - path: "python-backend/lib/minimodem.py"
      provides: "libminimodem_simple.so built for the Pi (ALSA/Pulse) and loadable by the binding"
      contains: "libminimodem_simple"
  key_links:
    - from: "AHK frontend (minimodem_simple.dll)"
      to: "Pi backend (libminimodem_simple.so)"
      via: "FSK audio over the USB cable at matching baud"
      pattern: "minimodem"
---

<objective>
Validate the full minimodem cutover end-to-end and retire the ggwave transport artifacts. Build `libminimodem_simple.so` on the Pi, run the cross-machine round trip, prove CRC-protected full-retransmit on induced corruption, sweep baud without recompile, verify the dependency-free DLL, and remove the stale `ggwave_simple.dll`/`SDL2.dll` once the round trip is confirmed.

Purpose: These are the VALIDATION.md Manual-Only verifications — they require two machines + a USB audio cable (and induced noise) and cannot be automated. This plan is the phase gate: both ends interoperate, integrity holds, and the old transport is gone.
Output: A verified, interoperating minimodem transport on both ends; ggwave artifacts retired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-CONTEXT.md
@.planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-RESEARCH.md
@.planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-VALIDATION.md
@minimodem-wrapper/CMakeLists.txt
@minimodem-wrapper/build_deps.sh
@python-backend/lib/minimodem.py
@python-backend/backend.py
</context>

<artifacts_this_phase_produces>
NEW / RETIRED in this plan:
- `libminimodem_simple.so` built on the Pi (ALSA/Pulse backend) — loaded by `lib/minimodem.py`
- RETIRED: `AHK/ggwave_simple.dll`, `AHK/SDL2.dll` (removed after the round trip is confirmed)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Build libminimodem_simple.so on the Pi and run the cross-machine round trip</name>
  <read_first>
    - minimodem-wrapper/CMakeLists.txt (the Linux branch: find fftw3f + libasound/libpulse, link pthread; the .so target)
    - minimodem-wrapper/build_deps.sh, minimodem-wrapper/build_wrapper.sh (Linux path: apt libfftw3-dev libasound2-dev)
    - python-backend/lib/minimodem.py (the ctypes .so load path the Pi binding expects)
    - python-backend/backend.py (--baud, --list, transport init)
    - .planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-VALIDATION.md (Manual-Only: cross-machine round trip; calibrate volume per direction first)
    - .planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-RESEARCH.md (§ FFTW Linux build; Threading Pitfall 3 half-duplex on speaker/mic; § Baud/Tone default 1200)
  </read_first>
  <files>python-backend/lib/minimodem.py</files>
  <action>
    On the Pi: `sudo apt-get install libfftw3-dev libasound2-dev`, then build the wrapper as `libminimodem_simple.so` (CMake Linux branch — ALSA/Pulse backend, dynamic libfftw3f is fine on Linux per RESEARCH). Confirm `lib/minimodem.py` resolves and loads the `.so` (adjust only the `.so` path-resolution in the binding if needed — that is the only file this plan edits on the Python side). Then run the round trip: start `backend.py --baud 1200 -i <pi-in> -o <pi-out>` on the Pi (use `--list` to find device indices) and the AHK frontend on Windows with `BAUD_RATE := 1200`, selecting the USB-audio devices. Calibrate volume per direction independently first (CLAUDE.md hardware setup). Send a radiologist draft from the frontend; confirm the formatted report returns intact and is displayed (no `__NOT_DOCUMENTED__`-surfacing bugs from transport). Watch for half-duplex bleed on speaker/mic (Pitfall 3) — note whether read-and-discard during TX sufficed or whether TX-pause-RX was needed.
  </action>
  <what-built>The Pi-side `libminimodem_simple.so` and a live cross-machine link. Requires two machines (Windows PC + Pi), the USB audio cable, and physical audio devices.</what-built>
  <how-to-verify>
    1. On the Pi: build `.so`, run `python backend.py --list` to find device indices, then `python backend.py --baud 1200 -i <in> -o <out>`.
    2. On Windows: run the AHK frontend (`BAUD_RATE := 1200`), select the USB audio interface for speaker + mic.
    3. Send a draft from the frontend; confirm a formatted report returns and displays intact within the expected ~4 s (1200 baud) window.
    4. Check `ggwave_log.txt` (AHK) and `backend_log.txt` (Pi) for matching SEND/RECV/CRC log entries and no CRC-mismatch retransmit loop on clean sends.
  </how-to-verify>
  <verify>
    <automated>grep -q "libminimodem_simple" python-backend/lib/minimodem.py</automated>
  </verify>
  <resume-signal>Type "approved" once a draft round-trips to an intact formatted report at 1200 baud, or describe the failure (and any half-duplex bleed observed).</resume-signal>
  <acceptance_criteria>
    - `libminimodem_simple.so` builds on the Pi and loads via `lib/minimodem.py`.
    - A draft sent from the AHK frontend returns an intact formatted report over the USB cable at matching baud (1200).
    - Logs on both ends show matching message IDs and no spurious CRC-mismatch retransmit loop on clean transmission.
  </acceptance_criteria>
  <done>The two ends interoperate over minimodem FSK end-to-end at the default baud.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Report-length + induced-corruption integrity test and baud sweep</name>
  <read_first>
    - python-backend/lib/chunking.py + AHK/include/chunking.ahk (the CRC verify + full-retransmit path from Plans 03/04)
    - .planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-VALIDATION.md (Manual-Only: report-length payload + corruption; baud sweep)
    - .planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-RESEARCH.md (§ Baud/Tone table + Landmine 1 high-baud tones above passband / ASSUMPTION A2; Pitfall 6)
    - .planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-CONTEXT.md (Integrity: never surface partial; Baud: tunable without recompile, both ends must match)
  </read_first>
  <files>python-backend/lib/minimodem.py</files>
  <action>
    No code changes expected (this is validation; touch `lib/minimodem.py` only if a path/tuning fix is required). Run two manual verifications:
    1. Integrity: send a ~500-byte report-length payload as a single frame; confirm clean delivery, then induce corruption (e.g. briefly disturb the cable / inject noise, or temporarily flip a byte before CRC check in a throwaway test build) and confirm the receiver detects the CRC mismatch, requests a FULL-message retransmit, and NEVER displays a partial/corrupt report (CLAUDE.md medico-legal rule).
    2. Baud sweep: with NO recompile, set `BAUD_RATE`/`--baud` to 1200, 4800, 9600 (matched on both ends each time) and confirm transmission works where the interface passband allows; then deliberately MISMATCH the baud and confirm the link does NOT decode (proving both ends must match). Record the baud at which the interface fails to acquire carrier (ASSUMPTION A2 / Pitfall 6) and whether a mark/space tone override was needed above ~4800.
  </action>
  <what-built>Two manual integrity/throughput verifications over the live link. Requires the cable, a way to induce corruption, and the ability to change baud at runtime.</what-built>
  <how-to-verify>
    1. Send a ~500-byte report; confirm intact delivery. Induce corruption; confirm CRC mismatch → full retransmit → no partial report ever shown; logs show RECV_FAIL(CRC) + RETX.
    2. Matched baud sweep 1200/4800/9600 (no recompile, just config/CLI) — note where it works and where high-baud carrier fails. Then mismatch baud and confirm NO decode.
  </how-to-verify>
  <verify>
    <automated>grep -q "libminimodem_simple" python-backend/lib/minimodem.py</automated>
  </verify>
  <resume-signal>Type "approved" once corruption triggers full retransmit with no partial report AND the baud sweep behaves as expected (matched works, mismatched fails), or describe what failed and at which baud.</resume-signal>
  <acceptance_criteria>
    - A ~500-byte single-frame report delivers intact; induced corruption produces a CRC mismatch → full retransmit, never a partial/corrupt report.
    - Baud is changeable without recompile; matched ends interoperate, mismatched ends do not decode.
    - The high-baud failure point and any tone-override need (A2) are recorded in the summary.
  </acceptance_criteria>
  <done>Integrity (CRC + full retransmit) and configurable-baud-both-ends-matched are validated on hardware.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Verify dependency-free DLL and retire ggwave_simple.dll + SDL2.dll</name>
  <read_first>
    - minimodem-wrapper/collect_dlls.sh (the ntldd zero-dependency gate from Plan 01)
    - .planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-VALIDATION.md (Manual-Only: dependency-free DLL via dependency walker)
    - .planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-RESEARCH.md (§ Runtime State Inventory: retire AHK/ggwave_simple.dll + AHK/SDL2.dll after cutover verified; Pitfall 5)
    - AHK/main_dll.ahk (confirm it now loads minimodem_simple.dll only — from Plan 03)
  </read_first>
  <files>AHK/ggwave_simple.dll, AHK/SDL2.dll</files>
  <action>
    Run the final dependency check: `ntldd -R AHK/minimodem_simple.dll` (or Dependencies.exe) and confirm ONLY Windows system DLLs appear (no `libgcc*`/`libwinpthread*`/`libfftw3f*` — Pitfall 5). ONLY AFTER Tasks 1-2 confirmed the round trip works, retire the stale transport artifacts: delete `AHK/ggwave_simple.dll` and `AHK/SDL2.dll` (the frontend now loads `minimodem_simple.dll` per Plan 03; these are no longer referenced). Confirm the AHK frontend still launches and round-trips after the deletion (quick re-run of Task 1's send). Do not delete the ggwave source tree or `ggwave-wrapper/` (kept for reference/rollback); only the stale runtime DLLs in `AHK/` are removed.
  </action>
  <what-built>Final dependency verification and removal of the retired ggwave runtime DLLs. Requires the Windows dependency walker and a confirmed working link.</what-built>
  <how-to-verify>
    1. `ntldd -R AHK/minimodem_simple.dll` shows only system DLLs (winmm, kernel32, msvcrt, user32, advapi32).
    2. Delete `AHK/ggwave_simple.dll` and `AHK/SDL2.dll`.
    3. Re-launch the AHK frontend and send one draft; confirm it still round-trips (proving nothing depended on the deleted DLLs).
  </how-to-verify>
  <verify>
    <automated>test ! -f AHK/ggwave_simple.dll && test ! -f AHK/SDL2.dll</automated>
  </verify>
  <resume-signal>Type "approved" once the dependency walker shows only system DLLs and the frontend still round-trips after removing the stale DLLs, or describe the dependency/launch failure.</resume-signal>
  <acceptance_criteria>
    - `ntldd -R` on `minimodem_simple.dll` shows only Windows system DLLs (FFTW-01 / Pitfall 5).
    - `AHK/ggwave_simple.dll` and `AHK/SDL2.dll` are removed.
    - The frontend launches and round-trips after removal (no residual dependency on the deleted DLLs).
  </acceptance_criteria>
  <done>The DLL is proven self-contained and the ggwave runtime artifacts are retired; the cutover is complete.</done>
</task>

</tasks>

<verification>
- Cross-machine round trip intact at matching baud (both ends interoperate).
- CRC mismatch → full retransmit, never a partial report (medico-legal integrity).
- Baud configurable without recompile; matched works, mismatched does not.
- `minimodem_simple.dll` dependency-free; ggwave_simple.dll + SDL2.dll retired.
</verification>

<success_criteria>
- The minimodem transport is validated end-to-end on real hardware with CRC32 integrity and configurable baud, and the ggwave transport artifacts are removed.
</success_criteria>

<output>
Create `.planning/phases/07-replace-ggwave-with-minimodem-both-sides/07-05-SUMMARY.md` when done.
</output>
